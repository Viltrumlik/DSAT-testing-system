/**
 * Testing Simulation — the SAT exam runner.
 *
 * Public surface for the rest of the app. Route files should import only from
 * here. Internals (hooks, services, state, components) are implementation
 * details and may change freely.
 */
export { ExamRunnerPage } from "./pages/ExamRunnerPage";
export type { Attempt, ExamQuestion, AttemptState } from "./types";
